
{% extends 'base.html.twig' %}

{% block title %}Hello ToolsController!{% endblock %}

{% block body %}
<div class="col-lg-12  contenu-blanc shadow-lg">

		<h1>Tools</h1>
		<table class="table table-striped table-advance table-hover">
			<thead>
				<tr class="bg-entete">
					<th class="col-md-1">Table</th>
					<th class="col-md-1">Action</th>
					
				</tr>
				{% if button is defined %}
					<button type="submit" class="btn btn-primary">
						{{ button }}
					</button>
				{% endif %}
				<div class="container-fluid contenu">
					
					</thead><br>
					<tbody>

						
							<tr>
								<td>Tours</td>
								
								<td
									class="row">

									<!-- formulaire pour demander la modification ou la suppression -->
									
										<form>
											<button type="" class="btn btn-primary btn-sm" formaction="" title="Edit">
												<i title="Edit">
													<i class="fa fa-pencil"></i>
													<a href = "{{path('adminTour')}}" class="text-white">Edit</a></button>
												{# <input
												type="hidden" name="_token" value="{{ csrf_token('action-item' ~ exam.id) }}"> #}
											{#  ~     pour concaténer des chaines en twig #}
											</form>
										
									</td>
								</td>
							</tr>
						
                            <tr>
								<td>Gallery</td>
								
								<td
									class="row">

									<!-- formulaire pour demander la modification ou la suppression -->
									
										<form>
											<button type="" class="btn btn-primary btn-sm" formaction="" title="Edit">
												<i title="Edit">
													<i class="fa fa-pencil"></i>
													<a href = "{{path('adminGallery')}}" class="text-white">Edit</a></button>
												{# <input
												type="hidden" name="_token" value="{{ csrf_token('action-item' ~ exam.id) }}"> #}
											{#  ~     pour concaténer des chaines en twig #}
											</form>
										
									</td>
								</td>
							</tr>

							<tr>
								<td>Requests</td>
								
								<td
									class="row">

									<!-- formulaire pour demander la modification ou la suppression -->
									
										<form>
											<button type="" class="btn btn-primary btn-sm" formaction="" title="Edit">
												<i title="Edit">
													<i class="fa fa-pencil"></i>
													<a href = "{{path('adminTourRequest')}}" class="text-white">Check</a></button>
												{# <input
												type="hidden" name="_token" value="{{ csrf_token('action-item' ~ exam.id) }}"> #}
											{#  ~     pour concaténer des chaines en twig #}
											</form>
										
									</td>
								</td>
							</tr>
					</tbody>

				</table>
				<div class="d-flex justify-content-center">
					
				</div>
			
		</div>
	</div>
{% endblock %}


